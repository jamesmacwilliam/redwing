<h1>Listing milestone_records</h1>

<table>
  <tr>
    <th>Jobs</th>
    <th>Milestone tasks</th>
    <th>Date completed</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @milestone_records.each do |milestone_record| %>
  <tr>
    <td><%= milestone_record.jobs_id %></td>
    <td><%= milestone_record.milestone_tasks_id %></td>
    <td><%= milestone_record.date_completed %></td>
    <td><%= link_to 'Show', milestone_record %></td>
    <td><%= link_to 'Edit', edit_milestone_record_path(milestone_record) %></td>
    <td><%= link_to 'Destroy', milestone_record, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Milestone record', new_milestone_record_path %>
