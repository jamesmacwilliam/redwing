<%= form_for(@milestone_record) do |f| %>
  <% if @milestone_record.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@milestone_record.errors.count, "error") %> prohibited this milestone_record from being saved:</h2>

      <ul>
      <% @milestone_record.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :jobs_id %><br />
    <%= f.text_field :jobs_id %>
  </div>
  <div class="field">
    <%= f.label :milestone_tasks_id %><br />
    <%= f.text_field :milestone_tasks_id %>
  </div>
  <div class="field">
    <%= f.label :date_completed %><br />
    <%= f.date_select :date_completed %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
